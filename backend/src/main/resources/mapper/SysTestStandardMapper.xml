<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.straykun.svd.svdsys.mapper.SysTestStandardMapper">

    <resultMap id="SysTestStandardResultMap" type="com.straykun.svd.svdsys.domain.SysTestStandard">
        <id property="id" column="id"/>
        <result property="projectType" column="project_type"/>
        <result property="gearLevel" column="gear_level"/>
        <result property="loadPercent" column="load_percent"/>
        <result property="limitMin" column="limit_min"/>
        <result property="limitMax" column="limit_max"/>
        <result property="thresholdItem" column="threshold_item"/>
    </resultMap>

    <select id="selectAll" resultMap="SysTestStandardResultMap">
        select id, project_type, gear_level, load_percent, limit_min, limit_max, threshold_item
        from sys_test_standard
        order by project_type, gear_level, load_percent
    </select>

    <select id="match" resultMap="SysTestStandardResultMap">
        select id, project_type, gear_level, load_percent, limit_min, limit_max, threshold_item
        from sys_test_standard
        where project_type = #{projectType}
          and gear_level = #{gearLevel}
          and load_percent = #{loadPercent}
        limit 1
    </select>

    <select id="matchAll" resultMap="SysTestStandardResultMap">
        select id, project_type, gear_level, load_percent, limit_min, limit_max, threshold_item
        from sys_test_standard
        where project_type = #{projectType}
          and gear_level = #{gearLevel}
          and load_percent = #{loadPercent}
        order by id
    </select>

    <select id="selectById" resultMap="SysTestStandardResultMap">
        select id, project_type, gear_level, load_percent, limit_min, limit_max, threshold_item
        from sys_test_standard
        where id = #{id}
        limit 1
    </select>

    <update id="updateById" parameterType="com.straykun.svd.svdsys.domain.SysTestStandard">
        update sys_test_standard
        set limit_min = #{limitMin},
            limit_max = #{limitMax}
        where id = #{id}
    </update>

</mapper>


